feat: Add Harness YAML parser

Task 3: Harness YAML Parser - Complete

## What was done:
- Created PipelineParser base class with abstract parse() method
- Implemented HarnessParser for Harness CI pipelines:
  - Parses stages (sequential and parallel)
  - Parses jobs with runner configuration
  - Parses steps (Run, Plugin, Background, Action types)
  - Handles caching configuration
  - Handles timeout parsing (10m, 1h, 30s formats)
  - Handles pipeline and stage variables
  - Infers sequential dependencies between stages
- Added get_parser() factory function
- Created ParseError for error handling

## Files created:
- src/pipelineiq/parsers/base.py (PipelineParser, ParseError)
- src/pipelineiq/parsers/harness.py (HarnessParser)
- tests/fixtures/harness/simple.yaml (test fixture)
- tests/fixtures/harness/complex.yaml (parallel stages, plugins)
- tests/test_parsers.py (15 tests)

## Files modified:
- src/pipelineiq/parsers/__init__.py (exports)

## Verification:
- pytest: 26 tests passed (15 new)
- Coverage: 82%

Next: Task 4 - DAG builder
