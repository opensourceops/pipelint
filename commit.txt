feat: Add parallelization rules

Task 6: Parallelization Rules - Complete

## What was done:
- Implemented ParallelStagesRule:
  - Uses DAG to find independent stages
  - Detects sequential stages that could run in parallel
- Implemented ParallelStepsRule:
  - Detects independent steps (lint/test/security patterns)
  - Suggests step grouping for parallel execution
- Added rules to default engine rule set

## Files created:
- src/pipelineiq/analyzers/rules/parallelization.py

## Files modified:
- src/pipelineiq/analyzers/__init__.py (exports)
- src/pipelineiq/core/engine.py (default rules)
- tests/test_analyzers.py (+5 tests)

## Verification:
- pytest: 20 analyzer tests passed
- Parallelization rules: 100% coverage

Next: Task 7 - More rules (timeout, redundant-clone, pinned-versions, resource)

